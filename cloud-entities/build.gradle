description = "cloud-entities"


bootJar {
    enabled = false
}


jar {
    enabled = true
}

dependencies {
    compile "com.baomidou:mybatis-plus-boot-starter:${myBatisPlusVersion}"
}

// 上传组件到nexus
uploadArchives {
    repositories {
        mavenLocal()
    }
}
